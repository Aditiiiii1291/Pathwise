# Database Configuration
DATABASE_URL=sqlite:///./pathwise.db

# Optional SMTP Email Alerts (Phase 13)
# If not configured, Pathwise operates in local in-app notification mode only.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=alerts@pathwise.edu
